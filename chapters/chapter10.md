# Chapter 10 — Melody

## Learning Objectives

By the end of this chapter, you should be able to:

- Explain what a melody is.
- Identify motifs, phrases, and sequences.
- Understand melodic contour.
- Recognize tension and release in melody.
- Understand how melody interacts with harmony.
- Write simple melodic ideas.

> 🎧 This chapter is interactive — hear every melodic example from the text played aloud, then build and play your own short melody in the Sketchpad at the end.

---

## Introduction

If harmony is the foundation of music, then **melody is the part you usually remember**.

A melody is a sequence of single notes arranged in a meaningful and expressive way. It is often the most recognizable element of a piece of music, whether it is a song you can sing or a theme from a film.

Melody works together with harmony and rhythm to create musical expression.

---

## What Is a Melody?

A **melody** is a linear sequence of notes perceived as a single musical idea. A good melody usually has a clear shape, a sense of direction, repetition and variation, and tension and release.

```
C  D  E  G  E  D  C
```

Even though it is just a sequence of notes, your brain groups it into a coherent idea.

---

## Motifs

A **motif** is the smallest recognizable musical idea. It is usually short (2–5 notes), repeated or varied, and used as a building block.

```
C  D  E
```

This motif can be repeated, transposed, or rhythmically altered. Many famous pieces are built from simple motifs developed over time.

---

## Phrases

A **phrase** is a complete musical thought, similar to a sentence in language. Typically a phrase lasts 4–8 measures (but not always) and ends with a sense of pause or resolution.

```
Phrase 1: C D E F | G A B C
Phrase 2: C B A G | F E D C
```

Music often uses **question and answer** phrasing: the first phrase feels unfinished (a question), and the second phrase resolves it (an answer).

---

## Sequences

A **sequence** is when a motif is repeated at different pitch levels.

```
C  D  E
D  E  F♯
E  F♯ G♯
```

Sequences create forward motion and development in melodies. They are commonly used in classical music, jazz improvisation, and film scoring.

---

## Melodic Contour

**Melodic contour** describes the overall shape of a melody. Common contours include ascending (rising), descending (falling), arch shape (rise then fall), and wave-like (multiple rises and falls).

```
Ascending:  C → D → E → F
Descending: F → E → D → C
Arch:       C → D → E → D → C
```

A good melody usually avoids being completely flat or random — it has direction and shape.

### 🎧 Try it — Melody Example Player

Every example above, played aloud. Pick one from the list and press play — this is the fastest way to connect the printed note names with what they actually sound like.

<div class="mt-widget" id="mt-example-widget">
  <div class="mt-widget-label">🎧 Melody Example Player</div>
  <label class="mt-field">
    Example
    <select id="mt-example-select" class="mt-select"></select>
  </label>
  <div class="mt-scale-row" id="mt-example-row"></div>
  <div class="mt-status" id="mt-example-status"></div>
  <button class="mt-btn" id="mt-example-play">▶ Play Example</button>
</div>

---

## Steps and Leaps

Melodies are built from two types of motion.

**Stepwise Motion** — moving between adjacent notes, e.g. **C → D → E → F**. Smooth, easy to sing, and common in melodies.

**Leaps (Skips)** — moving between non-adjacent notes, e.g. **C → G**, **E → B**, **A → F**. Adds interest and creates emotional impact, but must be balanced with stepwise motion.

---

## Consonance and Dissonance

Melodies often interact with harmony using consonant and dissonant intervals. **Consonance** is stable and pleasant; **dissonance** is tense and unresolved.

For example, a C over a C major chord is consonant, while a D over a C major chord is slightly dissonant (adding color). Good melodies balance consonance for stability with dissonance for tension and movement.

---

## Tension and Release in Melody

Melodies often follow a pattern of tension → release. Tension can come from large leaps, dissonant notes, higher register notes, and non-chord tones. Release happens when the melody resolves to chord tones or returns to stable notes like the tonic.

```
Melody:  E → F → G → E → C
Harmony: C major chord underneath
```

The motion toward C feels like resolution.

---

## Chord Tones vs Non-Chord Tones

Melodies often use two types of notes.

**Chord Tones** — notes that belong to the underlying chord. In a C major chord (C–E–G), these are C, E, and G. These sound stable.

**Non-Chord Tones** — notes that are not in the chord but add movement, such as passing tones, neighbor tones, and suspensions.

```
Chord: C major (C–E–G)
Melody: C → D → E
```

D is a **passing tone** — it connects two chord tones (C and E) by stepwise motion, without belonging to the chord itself. Select "Chord Tones vs. Passing Tone" in the widget above to hear this directly, with the chord sustaining underneath the melody.

---

## Repetition and Variation

Good melodies balance **repetition** (familiarity) and **variation** (interest).

```
C D E | C D E | C E D | C D E
```

Too much repetition becomes boring. Too much variation becomes confusing.

---

## Call and Response

Many melodies use a **call and response** structure: a call presents a musical idea (question), and a response offers a contrasting idea (answer).

```
Call:     C D E F
Response: G F E C
```

This structure is common in blues, jazz, gospel, and African and folk traditions.

---

## Writing a Simple Melody

A simple method for writing melodies:

1. Choose a scale (e.g., C major).
2. Start and end on the tonic.
3. Use mostly stepwise motion.
4. Add occasional leaps for interest.
5. Repeat and vary a motif.
6. Match important notes with chord tones.

```
C  D  E  G  E  D  C
```

This melody starts and ends on C, uses mostly steps, includes one leap (E → G), and feels complete and balanced.

### 🎚️ Try it — Melody Sketchpad

Now write your own. Pick a key, then click scale-degree buttons in any order to build up to 12 notes. Play it back, and try to follow the same advice as above: start and end on the tonic (degree 1), mostly step between neighbors, and use the occasional leap for interest.

<div class="mt-widget" id="mt-sketchpad-widget">
  <div class="mt-widget-label">🎚️ Melody Sketchpad</div>
  <label class="mt-field">
    Key
    <select id="mt-sketch-key-select" class="mt-select"></select>
  </label>
  <div class="mt-chord-row" id="mt-sketch-degree-row"></div>
  <div class="mt-scale-row" id="mt-sketch-melody-row"></div>
  <div class="mt-status" id="mt-sketch-status">Click scale degrees above to start composing.</div>
  <button class="mt-btn" id="mt-sketch-play">▶ Play My Melody</button>
  <button class="mt-btn" id="mt-sketch-example">💡 Load Example (C D E G E D C)</button>
  <button class="mt-btn" id="mt-sketch-clear">🗑 Clear</button>
</div>

---

## Melody and Harmony Relationship

Melody and harmony are deeply connected. A melody usually fits within a key, emphasizes chord tones on strong beats, and uses non-chord tones on weak beats.

```
Chord: C major
Melody: E → D → C → G
```

The melody sounds coherent because it aligns with harmonic structure.

---

## Practice Examples

### Example 1
What is a motif?
**Answer:** A short musical idea that serves as a building block for melodies.

### Example 2
What is melodic contour?
**Answer:** The overall shape or direction of a melody.

### Example 3
What is the difference between stepwise motion and a leap?
**Answer:** Stepwise motion moves between adjacent notes; a leap skips one or more notes.

### Example 4
What is a phrase in music?
**Answer:** A complete musical thought, similar to a sentence.

### Example 5
What is a passing tone?
**Answer:** A non-chord tone that connects two chord tones by stepwise motion.

---

## Key Takeaways

- Melody is a sequence of single notes forming a musical idea.
- Motifs are small building blocks of melodies.
- Phrases are complete musical thoughts.
- Sequences repeat ideas at different pitch levels.
- Melodic contour describes the shape of a melody.
- Stepwise motion is smooth; leaps add contrast.
- Tension and release give melodies emotional direction.
- Melodies interact with harmony using chord tones and non-chord tones.
- Good melodies balance repetition and variation.

---

## Summary

Melody is the most recognizable part of music and often what listeners remember first. In this chapter, you learned how melodies are built from motifs, phrases, and sequences, how melodic contour shapes musical ideas, and how stepwise motion and leaps create movement. You also explored how melody interacts with harmony through chord tones, non-chord tones, and tension and release.

In the next chapter, you will learn about **keys and modulation**, which explain how music can shift between tonal centers while maintaining coherence.

<script>
(function () {
  "use strict";

  if (!window.MTAudio) {
    console.error("MTAudio engine not found — make sure js/audio-engine.js is loaded in index.html");
    return;
  }
  var playTone = window.MTAudio.playTone;
  var midiToFreq = window.MTAudio.midiToFreq;
  var NOTE_NAMES = window.MTAudio.NOTE_NAMES;

  function noteName(midi) { return NOTE_NAMES[((midi % 12) + 12) % 12]; }

  // ---------- Melody Example Player ----------
  var exampleWidget = document.getElementById("mt-example-widget");
  if (exampleWidget) {
    var exampleSelect = document.getElementById("mt-example-select");
    var exampleRow = document.getElementById("mt-example-row");
    var exampleStatus = document.getElementById("mt-example-status");

    var EXAMPLES = {
      motif:       { label: "Motif (C D E)", type: "single", notes: [0, 2, 4] },
      sequence:    { label: "Sequence (motif repeated higher each time)", type: "groups", groups: [[0, 2, 4], [2, 4, 6], [4, 6, 8]] },
      phrase:      { label: "Phrase: Question & Answer", type: "groups", groups: [[0, 2, 4, 5, 7, 9, 11, 12], [12, 11, 9, 7, 5, 4, 2, 0]] },
      contour:     { label: "Melodic Contour: Arch", type: "single", notes: [0, 2, 4, 2, 0] },
      stepsLeaps:  { label: "Steps, then a Leap", type: "groups", groups: [[0, 2, 4, 5], [0, 7]] },
      callResponse:{ label: "Call & Response", type: "groups", groups: [[0, 2, 4, 5], [7, 5, 4, 0]] },
      passingTone: { label: "Chord Tones vs. Passing Tone", type: "layered", chord: [-12, -8, -5], melody: [0, 2, 4] },
      finalMelody: { label: "Complete Simple Melody", type: "single", notes: [0, 2, 4, 7, 4, 2, 0] }
    };

    Object.keys(EXAMPLES).forEach(function (key) {
      var opt = document.createElement("option");
      opt.value = key;
      opt.textContent = EXAMPLES[key].label;
      exampleSelect.appendChild(opt);
    });

    function renderExample() {
      var ex = EXAMPLES[exampleSelect.value];
      exampleRow.innerHTML = "";
      var rootMidi = 60;
      if (ex.type === "single") {
        ex.notes.forEach(function (o) {
          var chip = document.createElement("div");
          chip.className = "mt-scale-chip";
          chip.textContent = noteName(rootMidi + o);
          exampleRow.appendChild(chip);
        });
      } else if (ex.type === "groups") {
        ex.groups.forEach(function (group, gi) {
          group.forEach(function (o) {
            var chip = document.createElement("div");
            chip.className = "mt-scale-chip";
            chip.textContent = noteName(rootMidi + o);
            exampleRow.appendChild(chip);
          });
          if (gi < ex.groups.length - 1) {
            var sep = document.createElement("div");
            sep.className = "mt-scale-step";
            sep.textContent = "/";
            exampleRow.appendChild(sep);
          }
        });
      } else if (ex.type === "layered") {
        var chordLabel = document.createElement("div");
        chordLabel.className = "mt-scale-step";
        chordLabel.textContent = "chord:";
        exampleRow.appendChild(chordLabel);
        ex.chord.forEach(function (o) {
          var chip = document.createElement("div");
          chip.className = "mt-scale-chip";
          chip.textContent = noteName(rootMidi + o);
          exampleRow.appendChild(chip);
        });
        var melodyLabel = document.createElement("div");
        melodyLabel.className = "mt-scale-step";
        melodyLabel.textContent = "melody:";
        exampleRow.appendChild(melodyLabel);
        ex.melody.forEach(function (o) {
          var chip = document.createElement("div");
          chip.className = "mt-scale-chip";
          chip.textContent = noteName(rootMidi + o);
          exampleRow.appendChild(chip);
        });
      }
      exampleStatus.textContent = ex.label + ".";
    }

    exampleSelect.addEventListener("change", renderExample);

    document.getElementById("mt-example-play").addEventListener("click", function () {
      var ex = EXAMPLES[exampleSelect.value];
      var rootMidi = 60;
      if (ex.type === "single") {
        ex.notes.forEach(function (o, i) { playTone(midiToFreq(rootMidi + o), i * 0.4, 0.36); });
      } else if (ex.type === "groups") {
        var t = 0;
        ex.groups.forEach(function (group) {
          group.forEach(function (o, i) { playTone(midiToFreq(rootMidi + o), t + i * 0.4, 0.36); });
          t += group.length * 0.4 + 0.35;
        });
      } else if (ex.type === "layered") {
        ex.chord.forEach(function (o) { playTone(midiToFreq(rootMidi + o), 0, 1.6, "sine", 0.14); });
        ex.melody.forEach(function (o, i) { playTone(midiToFreq(rootMidi + o), i * 0.45, 0.4); });
      }
    });

    renderExample();
  }

  // ---------- Melody Sketchpad ----------
  var sketchWidget = document.getElementById("mt-sketchpad-widget");
  if (sketchWidget) {
    var sketchKeySelect = document.getElementById("mt-sketch-key-select");
    var degreeRow = document.getElementById("mt-sketch-degree-row");
    var melodyRow = document.getElementById("mt-sketch-melody-row");
    var sketchStatus = document.getElementById("mt-sketch-status");

    NOTE_NAMES.forEach(function (name, i) {
      var opt = document.createElement("option");
      opt.value = i;
      opt.textContent = name + " Major";
      sketchKeySelect.appendChild(opt);
    });

    var MAJOR_SCALE_OFFSETS = [0, 2, 4, 5, 7, 9, 11, 12];
    var melody = [];

    function renderDegreeButtons() {
      var rootOffset = parseInt(sketchKeySelect.value, 10);
      var rootMidi = 60 + rootOffset;
      degreeRow.innerHTML = "";
      MAJOR_SCALE_OFFSETS.forEach(function (o, i) {
        var midi = rootMidi + o;
        var btn = document.createElement("button");
        btn.className = "mt-btn";
        btn.textContent = (i + 1) + ": " + noteName(midi);
        btn.addEventListener("click", function () {
          if (melody.length >= 12) return;
          melody.push(midi);
          renderMelody();
        });
        degreeRow.appendChild(btn);
      });
    }

    function renderMelody() {
      melodyRow.innerHTML = "";
      if (melody.length === 0) {
        sketchStatus.textContent = "Click scale degrees above to start composing.";
        return;
      }
      melody.forEach(function (midi, i) {
        var chip = document.createElement("div");
        chip.className = "mt-scale-chip";
        chip.id = "mt-sketch-chip-" + i;
        chip.textContent = noteName(midi);
        melodyRow.appendChild(chip);
      });
      sketchStatus.textContent = melody.length + " note" + (melody.length === 1 ? "" : "s") + " so far.";
    }

    sketchKeySelect.addEventListener("change", function () {
      renderDegreeButtons();
    });

    document.getElementById("mt-sketch-play").addEventListener("click", function () {
      if (melody.length === 0) {
        sketchStatus.textContent = "Add a few notes first, then press play.";
        return;
      }
      melody.forEach(function (midi, i) {
        playTone(midiToFreq(midi), i * 0.4, 0.36);
        setTimeout(function () {
          document.querySelectorAll("#mt-sketch-melody-row .mt-scale-chip").forEach(function (c) {
            c.classList.remove("mt-scale-chip-active");
          });
          var chip = document.getElementById("mt-sketch-chip-" + i);
          if (chip) chip.classList.add("mt-scale-chip-active");
        }, i * 400);
      });
      setTimeout(function () {
        document.querySelectorAll("#mt-sketch-melody-row .mt-scale-chip").forEach(function (c) {
          c.classList.remove("mt-scale-chip-active");
        });
      }, melody.length * 400);
    });

    document.getElementById("mt-sketch-example").addEventListener("click", function () {
      var rootOffset = parseInt(sketchKeySelect.value, 10);
      var rootMidi = 60 + rootOffset;
      melody = [0, 2, 4, 7, 4, 2, 0].map(function (o) { return rootMidi + o; });
      renderMelody();
    });

    document.getElementById("mt-sketch-clear").addEventListener("click", function () {
      melody = [];
      renderMelody();
    });

    renderDegreeButtons();
    renderMelody();
  }
})();
</script>
